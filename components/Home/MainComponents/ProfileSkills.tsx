import React from "react";
import styles from "./ProfileSkills.module.css";

type Props = {};

const ProfileSkills = (props: Props) => {
    return <div className={styles['profile-card']}>ProfileSkills</div>;
};

export default ProfileSkills;
