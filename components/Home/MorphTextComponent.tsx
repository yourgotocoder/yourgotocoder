import React from "react";

type Props = {
    oldText: string;
    newText: string;
    transitionTime: number;
};

const MorphTextComponent = (props: Props) => {
    return <div>MorphTextComponent</div>;
};

export default MorphTextComponent;
